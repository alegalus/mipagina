/*window.onload = iniciar

function iniciar(){
        let btnSend = document.getElementById("btnSend")//con esto traigo el elemento del html, no el valor
        btnSend.addEventListener("click", clickBtnSend)
    }
    
    function clickBtnSend(){
        let txtName = document.getElementById("txtName")// con esto obtengo los datos del html
        let name = txtName.value//con esto el valor que se coloque
        let txtEmail = document.getElementById("txtEmail")
        let emal = txtEmail.value
        let txtMessage = document.getElementById("txtMessage")
        let message= txtMessage.value
        alert("hola")
    }*/
    